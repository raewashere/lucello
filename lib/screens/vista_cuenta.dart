import 'package:flutter/material.dart';

class vista_cuenta extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
          child: Text('Cuenta'),
      )
    );
  }
}